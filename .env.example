# Degasser Design MCP Server - Environment Variables

# Project root directory (absolute path)
# Used to resolve relative paths for databases, notebooks, etc.
DEGASSER_MCP_ROOT=C:\Users\hvksh\mcp-servers\degasser-design-mcp

# PHREEQC executable path
# Can be just "phreeqc" if on PATH, or full path to executable
PHREEQC_EXE=phreeqc

# MCP server timeouts (milliseconds)
MCP_TIMEOUT=600000
MCP_TOOL_TIMEOUT=600

# Simulation timeouts (seconds)
MCP_SIMULATION_TIMEOUT_S=600
PHREEQC_RUN_TIMEOUT_S=600

# Artifact management
# Set to 0 or false to disable artifact writing during development
MCP_WRITE_ARTIFACTS=1

# Logging configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOGLEVEL=INFO

# Optional: WaterTAP solver configuration
# IDAES_SOLVER=ipopt
# IPOPT_EXECUTABLE=/path/to/ipopt

# Optional: Parallel execution
# MCP_HYBRID_EXECUTOR_WORKERS=2

# Optional: Knowledge base configuration (if running locally)
# QDRANT_URL=http://localhost:6333
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=snowflake-arctic-embed:xs
# TEI_RERANK_URL=http://localhost:8087
